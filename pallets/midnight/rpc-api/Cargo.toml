[package]
name = "midnight-rpc-api"
version = "0.1.0"
edition = "2024"
license-file.workspace = true
description = "RPC trait and types for the Midnight pallet."

[dependencies]
jsonrpsee = { workspace = true, features = ["client", "macros"] }
serde.workspace = true
sp-storage = { workspace = true, default-features = false, features = ["std"] }

[lints]
workspace = true
